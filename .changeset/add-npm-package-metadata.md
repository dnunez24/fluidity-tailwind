---
"fluidity-tailwind": patch
---

Add npm package metadata: `repository`, `homepage`, `bugs`, and `publishConfig.access` fields, so the published npm page links back to the source repository and issue tracker.
