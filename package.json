{
  "name": "fluidity",
  "private": true,
  "scripts": {
    "build": "pnpm --filter fluidity-tailwind build",
    "build:check": "pnpm --filter fluidity-tailwind build:check",
    "test": "pnpm --filter fluidity-tailwind test",
    "docs:reference": "node scripts/build-docs-reference.mjs",
    "docs:dev": "pnpm --filter website dev",
    "docs:build": "pnpm --filter website build",
    "docs:preview": "pnpm --filter website preview",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build:check && pnpm test && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.12",
    "@changesets/cli": "^3.0.2"
  }
}
